# Deploying to Netlify

In this guide, we will deploy a to-do app built with Next.js and Tigris to Netlfiy.

## Prerequisites

- Github account, ([signup](https://github.com) for a free account if you
  don't have one.)

- Netlify account, ([signup](https://netlify.com) for a free account if you
  don't have one.)

- Tigris account, ([signup](https://www.tigrisdata.com/nextjs#signup-form) for a
  free account if you don't have one.)

## Setup

- Login to [Tigris console](https://console.preview.tigrisdata.cloud) and
  follow the video instruction to
  register a new application under `My Settings` > `Application Keys`.
  In the next step, we will use the generated `Client ID` and `Client Secret`
  as Environment Variables when deploying our Next.js app on Netlify.

import LiteYouTubeEmbed from "react-lite-youtube-embed";
import "react-lite-youtube-embed/dist/LiteYouTubeEmbed.css";

<LiteYouTubeEmbed
  id="Ls50EOrU3AQ"
  params="autoplay=1&autohide=1&showinfo=0&rel=0"
  title="Generate application keys for Tigris"
  poster="maxresdefault"
/>

- Login to your [netlify](https://netlify.com) account and go to
  [https://www.netlify.com/integrations/tigris](https://www.netlify.com/integrations/tigris).
- Click `Start with template`.
- Click `Deploy to Netlify`.
- You will be prompted to "Connect to Github". Click to connect.
- Enter your repository name and client credentials generated above.

![netlify_repo_setup](/img/screenshots/netlify_repo_setup.png)

(note: these parameters will be saved under your site's environment
variables - you can view them on Netlify at `Site Settings` -> `Environment variables`)

- Click on `Save and deploy`
- It will start deployment on Netlify. It may take 1-3 minutes while
  Netlify setup the site, custom domain and HTTPS for your site.
- Once setup, Netlify will generate URL for your website (if not using
  custom domain) and your Tigris + Next.js site is live on Netlify.

![netlify_site_setup](/img/screenshots/netlify_site_setup.png)

- 🎉🎉 You have just deployed a full stack website on netlify.
