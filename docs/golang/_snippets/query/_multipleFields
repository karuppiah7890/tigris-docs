it, err := catalog.Read(ctx, filter.And(filter.Eq("brand", "adidas"), filter.Lt("price", 50)))
if err != nil {
    panic(err)
}
var product Catalog
for it.Next(&product) {
    fmt.Printf("%+v\n", product)
}