it, err := catalog.Read(ctx, filter.And(filter.Eq("labels", "shoes"), filter.Gt("reviews.rating", 7)))
if err != nil {
    panic(err)
}

var product Catalog
for it.Next(&product) {
    fmt.Printf("%+v\n", product)
}