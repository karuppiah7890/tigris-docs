### Install Tigris CLI

Use the command below to install the CLI on **macOS**:

```shell
brew install tigrisdata/tigris/tigris-cli
```

Alternative ways of installation can be found [here](@site/docs/sdkstools/cli/installation.mdx).
